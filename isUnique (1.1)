#include <iostream>
#include <string>

bool isUnique(string str)
{
    if (name.length() > 128)
        return 0;
    else
     return 1;   
}

int main()
{
  std::string name;
  bool result;
  
  std::cout << "Enter a string to test: ";
  getline (std::cin, name);
  
  result = isUnique(name);
  
  if (result)
    std::cout << "Unique \n";
  else
    std::cout << "Not Unique \n";
  
}
